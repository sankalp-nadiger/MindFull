import Layout from '../Layout';
import ScheduleContent from './ScheduleContent';

const Schedule = () => {
  return (
    <Layout>
      <ScheduleContent />
    </Layout>
  );
};
export default Schedule;